name: Docker matrix
on: workflow_dispatch

jobs:
  test:
   runs-on: ${{matrix.os}}
   strategy:
     max-parallel: 1
     fail-fast: false
     matrix:
       os : [ubuntu-latest,windows-latest]
       images: [hello-world, alpine,sunildevops]
   steps:
     - name:  Docker details
       run: docker info
       
     - name: Run image
       run: docker run ${{matrix.images}}
     
