name: Matrix test-2
on: workflow_dispatch

jobs:
  test:
   runs-on: ${{matrix.os}}
   strategy:
     matrix:
       os: [ubuntu-latest, windows-latest]
       node: [16,18]
   steps:
     - name: show values
       run: |
         echo "os = ${{matrix.os}}"
         echo "node version = ${{matrix.node}}"
