name: jobs exec testing
on: workflow_dispatch

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: job_1 step
        run: echo "this is job1 running"

  job2:
     runs-on: ubuntu-latest
     needs: job1
     steps:
       - name: job_2 step
         run: echo "this is job2 running"

  job3:
     runs-on: ubuntu-latest
     needs: job2
     steps:
       - name: job_3 step
         run: echo "this is job3 running"       
    
